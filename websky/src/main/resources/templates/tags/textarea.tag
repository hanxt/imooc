

<div class="form-group com-md-12 col-sm-12" style="padding-right:45px">
    <label class="control-label col-md-2 col-sm-2 " for="${id}">${label}</label>
    <div class="col-md-10 col-sm-10 ">
        <textarea rows="${rows}"  id="${id}" name="${id}" ${readonly!} maxlength="${maxlength!}"
                   class="form-control">${value!}</textarea>
    </div>
</div>
